{
  "name": "chat-app",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "(cd chat-app-api && npm install) && (cd ..) && (cd vue-chat-app && npm install) && (cd ..)",
    "start": "npm-run-all -p start:api start:ui",
    "start:api": "cd chat-app-api && npm start",
    "start:ui": "cd vue-chat-app && npm run serve"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
